

class Infection():

    def __init__(self, infection_rate, infection_radius, isolation_rate) -> None:
        self.infection_rate = infection_rate
        self.infection_radius = infection_radius
        self.isolation_rate = isolation_rate
        